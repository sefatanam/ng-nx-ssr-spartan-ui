{
  "name": "ui-separator-helm",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "ui-separator-helm/src",
  "prefix": "hlm",
  "tags": [],
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/angular:ng-packagr-lite",
      "outputs": [
        "{workspaceRoot}/dist/{projectRoot}"
      ],
      "options": {
        "project": "ui-separator-helm/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "ui-separator-helm/tsconfig.lib.prod.json"
        },
        "development": {
          "tsConfig": "ui-separator-helm/tsconfig.lib.json"
        }
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "ui-separator-helm/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
